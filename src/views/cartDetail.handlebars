<dir>
    <div style="display: flex; flex-direction:row; align-items: center;">
    <a href="http://localhost:8080/chat"><button>Ir al Chat</button></a>
        <div style="margin-left: auto;">
            {{#if user}}
                <p>{{user.role}}: {{user.name}} / {{user.email}}</p>
                <button style=" float: right;" id="logout-btn">Log Out</button>
            {{else}}
                <a href="http://localhost:8080/login"><button>Log In</button></a>
                <a href="http://localhost:8080/register"><button>Register</button></a>
            {{/if}}
        </div>
    </div>

    <h1 style="text-align: center;">Cart Detail</h1>
    {{#if cart}}
        <strong>Carrito Id:{{cart._id}}</strong>
        <p>Estas llevando:</p>
        <ul style="list-style: none;">
        {{#each cart.products}}
            <li>{{this.quantity}} unidades de {{this.product.title}} - Precio unitario: ${{this.product.price}}</li>
        {{/each}}
        </ul>
    {{else}}
        <p>El carrito no fue encontrado.</p>
    {{/if}}
    <a href="http://localhost:8080/products"><button>Ver todos los productos</button></a>
</dir>
<script src="../js/logout.js"></script>